# 🌞 午间快讯生成 Prompt

## 任务

基于 inputs({DATE} 的 raw 采集),生成午间时段快讯,聚焦"早间之后新增的内容"。

## 数据范围

输入文件:
{INPUTS_LIST}

**报告范围**:**{DATE} 12:00 之后发布的 block**(block 标题行的 `MM-DD HH:MM` 字符串与 `{DATE}` 当日 `12:00` 字面比较,只看 ≥ 12:00 的)。早间已总结过的事件不重复,只列发酵和新增。

如果 12:00 之后没有任何 block,这本身就是"安静时段"信号(见 Q1-new 判定规则)。

## 定位

**这是一份可能"没啥好写"的报告**,但必须生成(用户明确要求:有没有都推)。

核心价值:**让用户知道"这段时间没发生大事"**,这本身就是一种信号 —— 可以放心专注干自己的活。

## 聚焦题目(午间版 4 题)

### Q1-new. 窗口内新增高声量事件

相较早间简报,窗口内有什么**新**冒出来的事件?

判定规则:
- **有新事件** → 正常写清楚
- **无新事件(仅是早间事件继续发酵)** → 明确写"窗口内无独立新事件,{早间事件} 继续扩散,新增声量 +N"
- **完全安静** → 写"📭 本时段 AI 圈相对安静,无重大新增动静"

### Q4-hot. 正在升温的争议

窗口内有没有 A vs B 观点对撞在发酵?
(可能是早间事件的延伸,也可能是全新议题)

输出:争议双方 + 最新进展 + 是否值得关注

### Q7-new. 新增案例故事

窗口内新增的"某人用 AI 做了 X"故事?
(通常是国内开发者上午发的分享,但以推文时间戳为准)

输出:故事主角 + 细节数据 + 传播度

### Q-alert. 窗口期动态

- 早间简报里提到的"中文首发窗口":是否已经有人写了?还在不在?
- 今天冒出来的新窗口:有没有新的 48h 选题?

## 输出格式

```markdown
# 🌞 AI Radar noon 快讯 — {DATE}

**报告范围**: {DATE} 12:00 之后 | **分析推文**: {N 条}

---

## 📊 本时段动态一览

{一段开场总结:安静/有新事件/某话题持续发酵,给用户 10 秒判断要不要细读}

---

## Q1. 新增高声量事件

{如无新事件,直接写"📭 无",不强行填}

---

## Q4. 正在升温的争议

{...}

---

## Q7. 新增案例故事

{...}

---

## ⏰ 窗口期动态

- 早间 Q14 的中文首发窗口({话题}): {是否还开着 / 已被 X 抢发 / 热度消退}
- 新出现的 48h 窗口: {如有}

---

## 🎯 午后建议

- {1-2 句话:基于新动态,下午要不要调整创作计划}
```

## 写作风格

- **极简**:整篇报告控制在 15 行内,没新事件就更短
- **判断清晰**:写清楚"有事 vs 无事",不要模糊其词
- **不要凑字数**:没内容就直说,让用户信任你的信号过滤能力
- **严格 12:00 切线**:早于 `{DATE} 12:00` 的推文即使很相关也不分析,那是早间简报的覆盖范围

## 写入方式

生成完整 markdown 后,**用 Bash heredoc 管道给 wiki-write 脚本**,它会对当日 wiki 的 `## section: {SECTION_NAME}` 段做幂等 upsert(其他 section 原样保留):

```bash
{WIKI_WRITE_CMD} <<'PERCH_EOF'
(把你上面"输出格式"里生成的完整 markdown 原样放这里,**不要**再包 `## section: {SECTION_NAME}` 外层标题,脚本会自动加)
PERCH_EOF
```

最终文件落在 `{WIKI_PATH}`(当日所有 section 共用一份)。不要自己用 Write 工具直接覆盖。
